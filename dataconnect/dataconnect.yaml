specVersion: "v1"
serviceId: "property-mock-dashboards"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "property-mock-dashboards-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
